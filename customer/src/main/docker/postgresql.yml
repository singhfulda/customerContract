version: '2'
services:
  customer-postgresql:
    image: postgres:12.3
    environment:
      - POSTGRES_USER=customer
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
